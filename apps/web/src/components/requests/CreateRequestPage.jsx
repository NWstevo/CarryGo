import PlaceholderPage from "../../components/common/PlaceholderPage";

export default function CreateRequestPage() {
  return (
    <PlaceholderPage
      title="Create request"
      description="Post a package request and connect with verified travelers."
    />
  );
}
